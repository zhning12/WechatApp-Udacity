.temperature {
  padding-top: 200rpx;
  text-align: center;
  font-size: 200rpx;
  /* 行高通常设置为字体的1.4倍，保证上下有空隙 */
  line-height: 280rpx;
  /* 加上透明度使得更好看 */
  opacity: 0.8;
}

.description{
  text-align: center;
  font-size: 40rpx;
  line-height: 56rpx;
  opacity: 0.65;
}

.wrapper{
	position: relative;
  height:760rpx;
  width: 750rpx;
}

.background-img{
	position: absolute;
	top:0;
	left: 0;
	width:100%;
	height: 100%;
	z-index: -1;
}
